import express from 'express';
const auth_router = express.Router();
// MUST BE IMPLEMENTED WITH /AUTH/ ENDPOINT

auth_router.get("/signin", (req, res) => {
});

export default auth_router; // export router